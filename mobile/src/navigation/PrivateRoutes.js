// Placeholder. La lógica RBAC se implementa en la tarea 6:
// leer el rol desde el JWT y decidir qué tabs ve cada perfil
// (Reportante, Supervisor, Técnico, Administrador).
import AppTabs from './AppTabs';

export default function PrivateRoutes() {
  return <AppTabs />;
}
